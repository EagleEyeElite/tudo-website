# 1/ Project name -------------------------------------------------
# Must be lower-case, no spaces and no invalid path chars.
# Will be used also as the WP database name
COMPOSE_PROJECT_NAME=tudo-website-dev

# 2/ Database user and password -----------------------------------------
# Set non-root database user if wanted (optional)
DATABASE_PASSWORD=password
DATABASE_USER=root

# 3/ For wordpress auto-install and auto-configuration -------------------
WORDPRESS_WEBSITE_TITLE="tudo-wordpress-cms-dev"

# URL
WORDPRESS_WEBSITE_URL="http://localhost:8080"
WORDPRESS_WEBSITE_URL_WITHOUT_HTTP="localhost:8080"

# Website admin identification. Specify a strong password
WORDPRESS_ADMIN_USER="admin"
WORDPRESS_ADMIN_PASSWORD="example-password"
WORDPRESS_ADMIN_EMAIL="your-email@example.com"

# 4/ Software versions -----------------------------------------------
WORDPRESS_VERSION=latest
MARIADB_VERSION=latest
PHPMYADMIN_VERSION=latest

# 5/ Ports: Can be changed, also update Wordpress URL ---------------------------
WORDPRESS_PORT=8080
PHPMYADMIN_PORT=8081
ACTIVITY_INDICATOR_KV_PORT=8082

# 7/ Healthcheck availability of host services (mysql and WordPress server)
# Waiting time in second
WAIT_BEFORE_HOSTS=5
WAIT_AFTER_HOSTS=5
WAIT_HOSTS_TIMEOUT=300
WAIT_SLEEP_INTERVAL=5
WAIT_HOST_CONNECT_TIMEOUT=5

#8/ Wordpress settings
WORDPRESS_NICKNAME="Development"
WORDPRESS_GRAPHQL_JWT_AUTH_SECRET_KEY="your-strong-secret"

## Activity Indicator stuff
# set activity indicator kv database config
KV_INTERFACE_TOKEN: your_kv_interface_token
